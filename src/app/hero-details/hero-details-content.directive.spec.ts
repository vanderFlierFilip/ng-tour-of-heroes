import { HeroDetailsContentDirective } from './hero-details-content.directive';

describe('HeroDetailsContentDirective', () => {
  it('should create an instance', () => {
    const directive = new HeroDetailsContentDirective();
    expect(directive).toBeTruthy();
  });
});
